import {config} from './config.ts';
import {getGeneString} from './utils.ts';

/* function createRabbit (oldRabbit: Entity) => Entity {
    
    const health: number = config.MAXRABBITHEALTH / 2;
    const sated: number = 0;
    const gene: string = getGeneString ();
    const genePointer: number = 0;
    const category: string = 'herbivore';
    const name: string = 'rabbit';
    const visible: boolean = false;
    const appearance = config.RABBITCHAR;
    
    return {
        
        health: () => health,
        sated: () => sated,
        gene: () => gene,
        category: () => category,
        name: () => name,
        visible: () => visible,
        appearance: () => appearance
        
    };
}; */