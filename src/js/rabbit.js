const settings = require ('./settings.js');
const animalArray = require ('./animalArray.js');
const mtk = require ('./mtk.js');

function createRabbit () {
    
    let health = settings.MAXRABBITHEALTH / 2;
    let sated = 0;
    const geneString = mtk.initGeneString ();
    
    
    
}